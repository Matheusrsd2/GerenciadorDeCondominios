@model IEnumerable<GerenciadorCondominios.Domain.Models.Veiculo>

<div class="collection with-header">
    <div class="collection-header">
        <div class="row">
            <div class="col s10">
                <h6>Meus veículos</h6>
            </div>
            <div class="col s2">
                <a asp-controller="Veiculos" asp-action="Create" class="waves-effect waves-light btn-small btn-floating green darken-4" data-position="right" data-tooltip="Novo veículo">
                    <i class="material-icons">add</i>
                </a>
            </div>
        </div>
    </div>

    <div class="collection-item">
        <table class="striped highlight reponsive">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Nome)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Marca)
                    </th>
             
                    <th>
                        @Html.DisplayNameFor(model => model.Placa)
                    </th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Nome)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Marca)
                        </td>
              
                        <td>
                            @Html.DisplayFor(modelItem => item.Placa)
                        </td>
                        <td>
                            <a asp-controller="Veiculos" asp-action="Edit" asp-route-id="@item.Id" class="btn-floating btn-small blue darken-4" data-position="right" data-tooltip="Atualizar veículo">
                                <i class="material-icons">mode_edit</i>
                            </a>

                            <a onclick="ConfirmarExclusao(@item.Id, '@item.Nome')" class="btn-floating btn-small red darken-4 modal-trigger" data-target="modal" data-position="right">
                                <i class="material-icons">delete_forever</i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

